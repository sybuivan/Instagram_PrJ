import React from 'react';
import { Grid } from '@mui/material';
const Saved = () => {
  return (
    <Grid item xs={12}>
      Page Saved
    </Grid>
  );
};

Saved.propTypes = {};

export default Saved;
